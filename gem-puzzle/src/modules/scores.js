import { BasicNode } from "../index";

const table = new BasicNode(undefined, undefined, undefined);
const row = new BasicNode(undefined, undefined, undefined);
const cell = new BasicNode(undefined, undefined, undefined);
